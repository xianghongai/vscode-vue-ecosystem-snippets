{
  "TanStack Vue Query import": {
    "prefix": "queryImport",
    "body": ["import { ${1:useQuery} } from '@tanstack/vue-query';", "$0"],
    "description": "Import a TanStack Vue Query 5 API.\n\n导入 TanStack Vue Query 5 API。",
    "scope": "vue,javascript,typescript"
  },
  "TanStack Vue Query plugin": {
    "prefix": "queryPlugin",
    "body": [
      "const ${1:queryClient} = new QueryClient({",
      "  defaultOptions: { queries: { staleTime: ${2:30_000} } },",
      "});",
      "",
      "app.use(VueQueryPlugin, { queryClient: ${1} });",
      "$0"
    ],
    "description": "Install TanStack Vue Query on the application. Requires QueryClient and VueQueryPlugin from @tanstack/vue-query.\n\n在应用上安装 TanStack Vue Query；需要 @tanstack/vue-query 的 QueryClient 与 VueQueryPlugin。",
    "scope": "javascript,typescript"
  },
  "TanStack Vue Query queryOptions": {
    "prefix": ["queryOptions", "qo"],
    "body": [
      "export const ${1:itemQuery} = (${2:id}) =>",
      "  queryOptions({",
      "    queryKey: ['${3:items}', ${2}],",
      "    queryFn: ({ signal }) => ${4:fetchItem}(toValue(${2}), { signal }),",
      "  });",
      "$0"
    ],
    "description": "Define reusable query options so key and fetcher stay together. Passing a ref or getter as the argument keeps the key reactive once toValue reads it. Requires queryOptions from @tanstack/vue-query and toValue from vue.\n\n定义可复用的查询选项，让键与请求函数绑在一起；实参传 ref 或 getter，经 toValue 读取后键即保持响应式；需要 @tanstack/vue-query 的 queryOptions 与 vue 的 toValue。",
    "scope": "javascript,typescript"
  }
}
